# Supabase — project POP400 (https://supabase.com/dashboard/project/dncwllpqoomdcovudmww)
# Copy to .env and add your anon key from Settings → API → Project API keys → anon public.

VITE_SUPABASE_URL=https://dncwllpqoomdcovudmww.supabase.co
VITE_SUPABASE_ANON_KEY=

# Product list for the app comes from Supabase table POPMAS when URL + anon key are set (see waitroseCatalog.ts).

# Google Vision OCR
# The service account credentials JSON is stored as an ENCRYPTED Supabase secret
# (GOOGLE_VISION_CREDENTIALS) — never add the raw JSON here or commit it.
# To deploy the edge function and set the secret, run:
#   export SUPABASE_ACCESS_TOKEN=<your-token>   # from supabase.com/dashboard/account/tokens
#   bash scripts/deploy-vision-ocr.sh
